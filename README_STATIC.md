# Полностью статический деплой на GitHub Pages

## ✅ Да, можно сделать все на GitHub Pages!

Проект теперь поддерживает **полностью статический режим** без использования backend сервера.

## Как это работает

1. **Данные в JSON файлах** → хранятся в репозитории (`public/data/data.json`)
2. **Frontend читает данные** → напрямую из JSON файлов
3. **Админка сохраняет данные** → через GitHub API (редактирует JSON файл в репозитории)
4. **Wotstat интеграция** → работает на клиенте (с CORS proxy при необходимости)

## Быстрый старт

### 1. Создайте репозиторий на GitHub

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/YOUR_USERNAME/wot-tournament.git
git push -u origin main
```

### 2. Настройте GitHub Pages

- Settings → Pages → Source: **GitHub Actions**
- Workflow уже настроен в `.github/workflows/deploy.yml`

### 3. Создайте Personal Access Token

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите scope: **repo** (полный доступ)
4. Скопируйте токен

### 4. Настройте админку

1. Зайдите в админку: `https://your-username.github.io/wot-tournament/admin/login`
2. Войдите (по умолчанию: `admin` / `admin`)
3. Перейдите в **Settings** → **GitHub Configuration**
4. Введите:
   - Repository Owner: ваш GitHub username
   - Repository Name: название репозитория
   - Branch: `main`
   - Personal Access Token: вставьте токен
5. Нажмите "Проверить подключение"

### 5. Готово!

Теперь вы можете:
- ✅ Просматривать данные на сайте
- ✅ Редактировать данные через админку
- ✅ Все изменения сохраняются в Git
- ✅ Полностью бесплатно на GitHub Pages

## Структура данных

Данные хранятся в `public/data/data.json`:

```json
{
  "players": [...],
  "teams": [...],
  "matches": [...],
  "rules": [...],
  "ruleCards": [...],
  "settings": {...}
}
```

## Ограничения и решения

### 1. CORS с Wotstat

**Проблема**: Wotstat может блокировать запросы с других доменов.

**Решение**: 
- Используется CORS proxy (`api.allorigins.win`)
- Или настройте GitHub Actions для периодического обновления данных

### 2. GitHub API Rate Limits

**Лимиты**:
- С токеном: 5000 запросов/час
- Без токена: 60 запросов/час

**Решение**: Используйте Personal Access Token в админке.

### 3. Безопасность токена

Токен хранится в `localStorage` браузера. Для личных проектов это приемлемо.

## Переключение между режимами

Проект автоматически определяет режим:
- Если `VITE_API_URL` не установлен → статический режим
- Если `VITE_USE_STATIC_DATA=true` → статический режим
- Иначе → использует API backend

## Преимущества

✅ **Полностью бесплатно**  
✅ **Нет зависимости от сторонних сервисов**  
✅ **Все данные в Git** (история изменений)  
✅ **Легко делать бэкапы** (клонировать репозиторий)  
✅ **Работает полностью на GitHub Pages**

## Дополнительная информация

- 📖 [GITHUB_PAGES_SETUP.md](./GITHUB_PAGES_SETUP.md) - Подробная инструкция
- 📖 [STATIC_MODE_README.md](./STATIC_MODE_README.md) - Технические детали


